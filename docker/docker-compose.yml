version: '3.9'
services:
  machine-vision:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ../config:/app/config
      - ../data:/app/data
